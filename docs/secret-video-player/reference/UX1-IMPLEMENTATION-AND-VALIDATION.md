# UX1：交互与功能优化实施与验证记录

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-09-15 |
| 文档版本 | v1.3 |
| 依据方案 | [UX1 实施修订 v1.1](../design/2026-09-15-ux-functional-optimization-v1.1.md)，保留初始 v1.0 |
| 当前状态 | UX1.1 / UX1.2 已实现，本地开发检查通过；SDK 依赖及人工体验边界见下文 |

## 1. 本轮实施约束

用户于 2026-09-15 授权按方案开始实施并分批提交 Git，同时明确：

1. SOLID 为首要设计约束，按变化原因区分界面、交互策略、应用协调与底层业务职责。
2. 设计模式朴素使用，优先组合已有服务，不引入通用框架或重复的业务状态。
3. 新增和修改的注释使用中文；涉及取消、并发、状态所有权、资源及兼容边界时解释设计思路。
4. 补齐本地单元测试和开发门禁，同步更新功能说明及本专用文档。
5. 不使用 AIFLOW，不使用 Windows CI，不执行发布门禁；发布阶段再执行相关门禁。
6. 每批提交包含具体的 Git title 和 description。测试结果只陈述实际执行证据。

本轮开发验证采用 Debug 构建、业务单元测试、Headless 界面测试和新增组件覆盖率；不调用 R1 Full、Host 打包验收、ReleaseAcceptance、Windows CI 或部署脚本。原生播放体验的本地专项验证与发布门禁分别记录。

## 2. 批次记录

| 批次 | 内容 | 状态 | 验证 |
| --- | --- | --- | --- |
| UX1.0 | 方案归档、开发约束与实施记录 | 完成 | 文档链接与格式检查 |
| UX1.1a | 播放意图、媒体库入口与历史展示 | 已实现并完成本地开发检查 | 251 项单元测试、6 项界面测试；续播策略行/分支 100% |
| UX1.1b | 批次检查、重试、编辑恢复和错误反馈 | 已实现并完成本地开发检查 | 全量 277 项单元、6 项界面通过；收尾回归见下文 |
| UX1.2a | 输入、统一输出、多选配置和结果操作 | 已实现并完成本地开发检查 | 293 项单元、7 项界面通过；输出策略行/分支 100% |
| UX1.2b | 最近目录、继续观看、播放控制及真实媒体恢复修正 | 已实现并完成本地开发检查 | 324 项单元、8 项界面通过；五个纯规则组件行/分支 100%；原生专项通过 |
| UX1.3 | 场景扩展候选 | 未纳入本次默认实施 | 按后续场景确定 |

## 3. 测试基线与结果

开始实施前运行本地业务测试采集基线；尚未完成的检查不计为通过。覆盖率使用本轮本地测试口径，不冒用包含宿主发布验收的 R1 全量覆盖率阈值。

### 3.1 UX1.1a 验证证据

- 改动前：233 项业务单元测试全部通过，0 跳过。
- 首轮本地开发检查：`TestResults/UX1/dev-80f2ab26ecc44a29932b0e1812ed0ac4/summary.json`，251 项单元测试、5 项原界面测试通过；新增 PlaybackResumePolicy 行/分支覆盖率均为 100%。
- 布局修正后：`TestResults/UX1/playback-ui-v3/ui.trx`，6 项界面测试通过，包含新增的 760 × 600 媒体库列表高度与目录入口断言。
- Skia/Headless 生成四种文档双主题截图，已检查媒体库与单文件播放器。窄窗口列表被挤压的问题通过压缩常驻区、将次级操作放入“更多”修正。
- 本次渲染不验证真实原生视频。窄窗口沿用现有覆盖侧栏行为，用户可通过左侧箭头收起侧栏。

本地开发检查入口：`pwsh -NoProfile -File scripts/Test-UX1.ps1`。脚本只运行 Tests / UiTests，要求测试非空、全部通过且无跳过；通过 `tests/ux1-coverage-policy.json` 验证新增纯策略覆盖率。

### 3.2 UX1.1b 验证证据

- 本地开发检查：`TestResults/UX1/dev-386e5d749a1348e5afec45fe616b104b/summary.json`，277 项单元测试、6 项界面测试全部通过，0 跳过，Debug 构建 0 警告。
- PlaybackResumePolicy、BatchInteractionPolicy 的行/分支覆盖率均为 100%。覆盖率针对纯交互策略，不能据此推断整个插件覆盖率。
- 重试验证涵盖限定所选项、跳过成功项、警告与输出改名后停留在检查结果、迟到进度不污染新任务。
- 编辑验证涵盖草稿取消不释放媒体，保存成功/失败恢复位置与播放意图，关闭或换片后不恢复旧目标。当时模拟会话以 Ready 保持不播放；真实解码器的准备与暂停状态差异已在 3.4 节修正。
- 收尾补充“重查失败不得执行旧计划”回归后，UX1 与 R1BatchViewModelTests 定向测试共 51 项通过。
- 文件占用与真实解码恢复仍需真实媒体验证；上述编辑测试使用可控存储及播放会话，不计作原生播放证据。

### 3.3 UX1.2a 验证证据与边界

- 开发检查：`TestResults/UX1/dev-621dcb5b88274ea196c9ec222fe6a3d1/summary.json`，293 项单元、6 项界面测试通过；三个纯规则组件行/分支均为 100%。
- 新增多选及完成项只读界面测试后，`TestResults/UX1/batch-ui-v2/ui.trx` 共 7 项通过。760 × 600 双主题截图位于 `batch-captures-v2`；检查了加解密空态及加密配置展开态。
- 导入覆盖递归开关、格式过滤、去重、缺失路径、取消、关闭及忙碌期间的迟到结果；输出覆盖平铺、目录保留、越界拒绝和成功项不被批量修改。
- 候选发现由 `IVideoInputDiscovery` 承担，`BatchImportViewModel` 协调取消与队列交付。密码及加解密不进入导入组件。
- `EncryptionOutputPolicy` 生成目录内的建议路径；真实目录创建、重名处理和写入继续交给现有预检及事务。保留结构时包含导入根目录名称，多个同名根的冲突继续由预检说明。
- 当前 SDK 3.4.0 的 `NewDocumentActivation` 只携带标题与创建意图；公开服务没有插件携带文件路径新建 Document 的接口。因此本批采用“定位文件 / 复制路径 → 播放器粘贴路径或拖入文件”，密码由目标 Document 单独输入。一键创建播放器标签留待公开 SDK 支持后实施，不调用 Host 内部服务。
- 文件定位和系统剪贴板的真实桌面交互未由 Headless 测试证明；目录扫描、输出规则和绑定行为已验证。没有执行 Windows CI 或发布门禁。

### 3.4 UX1.2b 与最终本地检查

- 最终开发检查：`TestResults/UX1/dev-efbf0f402bb941029eaa2c8e774df5e5/summary.json`。324 项单元测试、8 项界面测试全部通过，0 跳过；Debug 构建 0 警告、0 错误。
- PlaybackResumePolicy、BatchInteractionPolicy、EncryptionOutputPolicy、PlaybackTimePolicy、RecentFolderPolicy 行/分支覆盖率均为 100%。门禁要求这些纯规则最低行 90%、分支 80%，不将该结果表述为全插件 100%。
- 最近目录覆盖旧 JSON 字段缺失、规范化、去重、限长、移除后偏好更新不再加回；继续观看和定位复用原目录快照及历史，不发起媒体加载。
- 时间跳转覆盖三种格式、负数、非法字符、溢出、未知片长及片尾边界；静音覆盖恢复原音量。界面新增验证跳转弹层绑定及 `M` 不抢占输入框焦点。
- 本轮保存恢复的首个真实样本暴露 Ready/0ms 回退，已修正同一媒体事务内的冷解码器准备：临时静音，复用原表面恢复序列，定位后继续播放或发布真实 Paused 状态，刷新轨道。补测成功、失败、取消、旧媒体补偿及音量恢复。
- 恢复警告不会再被“加载成功”清除；媒体库和单文件来源显示本次回退说明。编辑器检查位置及控制恢复结果，失败仍保留明确反馈。
- 760 × 600 双主题图见 `TestResults/UX1/daily-captures-v1`；原生输出的视觉覆盖不由 Headless 截图证明。

### 3.5 本地原生专项

- 使用仓库已有 `synthetic-multitrack-subtitles.mp4`，在独立窗口中临时加密并打开，所有生成数据使用本次临时根，关闭后清理。
- 已验证来源主按钮真实起播；保存公开信息后恢复位置、暂停、倍速、音轨和字幕轨；静音与恢复原音量；760 × 600 全屏进出保留暂停位置；多标签切换后定位；再次按历史继续播放；关闭后文件可移动。
- 首个失败报告保留在 `TestResults/UX1/native-1741184ba376428eb0d39647d382dde7/report.json`；修正并加入轨道断言后的报告见 `TestResults/UX1/native-b2ad901263184264ac1ddc18bc758a45/report.json`，Passed=true。
- 专用场景以 `--smoke-ux1` 显式启用，保留原 `--smoke-media` 场景对旧样本的兼容；可复现入口为 `scripts/Test-UX1Native.ps1`。专用脚本实跑结果：`TestResults/UX1/native-f1c563734dd84f6ea8c62d23db152d32/report.json`，Passed=true，7 项实际检查完成。
- 这是独立窗口的程序化原生验证；宿主 Dock 拖拽重排、更多显示缩放/GPU、系统文件定位及剪贴板的人工操作仍需体验验收。运行日志中的原生诊断保留在对应目录，未用该专项替代发布门禁。

## 4. 设计与兼容说明

- `PlaybackResumePolicy` 是无状态规则，只判断历史数值和格式化时间，不读取文件或控制播放。
- `SingleVideoSourceViewModel` 通过扫描、历史存储和历史协调器窄接口接入续播；实际媒体加载使用现有原子播放接口。
- `LibraryDocumentCoordinatorViewModel` 保存最新待输入意图；取消提升代次、清除目标并取消令牌，迟到完成不能覆盖当前状态。
- `VideoLibraryItemViewModel` 将实时状态作为文档投影，历史快照与当前播放标识分别显示；只在状态或列表变化时更新标识。
- 界面主操作变更属于方案明确要求的产品行为调整，保留“仅加载”以承接原有暂停加载意图。
- 单文件和媒体库复用历史策略；密码及正在执行的请求归属各自 Document。
- `BatchInteractionPolicy` 只判断下一步提示及重试自动执行条件；队列协调器继续拥有检查、取消和执行代次。重新检查开始即废弃旧计划。
- `IPublicVideoInfoStore` 隔离公开信息读写；编辑器只协调草稿与恢复，默认适配器继续调用现有容器能力。
- 原加解密、输出事务、容器格式、稳定标识和 Workflow Action 继续复用。
- 现有 `.gitignore` 本地改动与本任务无关，不纳入提交。

## 5. 版本记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v1.0 | 2026-09-15 | 建立本轮开发约束、批次与证据记录 |
| v1.1 | 2026-09-15 | 记录 UX1.1 两批行为变化、测试证据与真实播放验证边界 |
| v1.2 | 2026-09-15 | 记录批次导入、多选输出、结果路径交接与 SDK 能力边界 |
| v1.3 | 2026-09-15 | 完成日常观看、目录管理、原生历史恢复及最终开发检查；记录能力与验证边界 |

## 6. 本地复现

在插件仓库根目录执行：

```powershell
# Debug 单元测试、Headless 界面测试及纯规则覆盖率；自动禁用插件部署。
pwsh -NoProfile -File scripts/Test-UX1.ps1

# 独立窗口真实媒体专项；需要本地 Windows x64 桌面和随项目配置的原生运行时。
pwsh -NoProfile -File scripts/Test-UX1Native.ps1
```

每次创建新的 `TestResults/UX1` 结果目录；原始 TRX、覆盖率和原生日志作为本地产物保留，不提交 Git。版本化文档保存命令与实际结果索引。

## 7. 后续边界

- SDK 尚未提供携带文件路径新建播放器 Document 的公开入口。本轮提供复制/粘贴及拖放交接，后续接口明确后再接一键新标签。
- 继续观看基于当前媒体库；跨目录独立播放列表属于 UX1.3 候选。
- 本轮未执行 AIFLOW、Windows CI、发布门禁、部署或推送；没有调整容器、加解密算法、依赖版本与插件发布版本。
